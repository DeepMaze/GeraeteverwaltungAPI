#!/usr/bin/env node

var app = require('../app');
var http = require('http');

var normalizePort = require('../helper/normalizePort');
var { onError, onListening } = require('../helper/serverEvents');
var { createLog, handleOldLogs } = require('../helper/logging');

var { generalConfig } = require('../environment/config');



var port = normalizePort(generalConfig.port);
app.set('port', port);

var server = http.createServer(app);

setInterval(() => {
    handleOldLogs();
}, 1000 * 60 * 60 * generalConfig.clearOldLogsInterval);

server.listen(port);
if (generalConfig.createLogs) { createLog({ type: 'serverStart', params: { port } }); }

server.on('error', onError);
server.on('listening', onListening);